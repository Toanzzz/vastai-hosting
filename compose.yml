services:
  vastai-hosting:
    image: vastai-hosting
    container_name: vastai-hosting
    restart: unless-stopped
    env_file: .env
    volumes:
      - telegram-state:/app/data

volumes:
  telegram-state:
